apiVersion: v1
kind: Service
metadata:
  name: vllm-external
  namespace: monitoring
  labels:
    app: vllm-external
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8000